# PoliScape 最新ウォークスルー ＆ 引き継ぎ書（2026-09-14更新）

> **注意**: このファイルは「常に最新の状態」を記録するメイン引き継ぎ書です。
> 新しいセッションや別端末のAIは、まずこのファイルを読んで現在地を把握してください。

---

## 直近で完了した作業サマリ

### 1. カテゴリ再設計（20 → 11カテゴリ統合）
- 全76ファイルの政策JSONカテゴリフィールドを更新
- `types/policy.ts`、`page.tsx` のカテゴリ設定を11種類に統一
- マイグレーションスクリプト `scripts/migrate-categories.js` で検証済み

### 2. 動的OGP画像のSSG化（ビルド時全500枚事前生成）
- `src/app/policies/[id]/opengraph-image.tsx` で `generateStaticParams` を実装
- Xのタイムアウト問題を完全解消（Vercel CDNから即時配信）
- 11カテゴリ別のジュエルトーン背景 ＋ 白ピルバッジ ＋ 天秤ロゴデザインを採用

### 3. ヘッダーリニューアル ＆ タグライン統一
- `Header.tsx`: ダークネイビー背景 × ティール天秤（Scale）アイコン、タイトル「PoliScape」
- `layout.tsx`: SEOメタデータ（OGP・Twitter含む）のタグラインを「公的データと客観的事実で知る、日本の政策カタログ」に完全統一

### 4. X（旧Twitter）投稿運用（Day 1〜8 予約済み）
- **Day 1〜8までの予約投稿をX公式の予約機能にて設定完了**
- 児童手当、年収の壁、新NISA、ライドシェア、年金開始年齢、自転車青切符など時事・関心の高いテーマを連続配信

### 5. 開発・運用体制の設計（ノートPC × デスクトップ × スマホ）
- **メイン開発（腰を据えた大改修）**: 爆速ノートPC（Ryzen AI 340）。作業後はGitHubにpushして電源OFF自由。
- **サブ開発・待機（24時間稼働）**: 自宅デスクトップ（Ryzen 3600 / 24GB）。LINE Bot受信サーバーを動かして常時待機。
- **指示・スキマ時間**: スマホのLINEから指示（アイデア記録、量産指示）。
- **情報の一元管理**: すべて `docs/` 配下のMarkdownとGitHubで同期。

---

## 現在のサイト状況・数値

| 項目 | 数値 / 状況 |
|------|------------|
| 登録済み政策データ | **500件** (JSON) |
| 根拠法データ | **53件** |
| 個別シミュレーター | **51種類** |
| カテゴリ | **11カテゴリ** |
| ビルド | **1062ページ SSG事前生成（エラー0）** |
| リポジトリ容量 | **約19.3 MB**（上限1GBに対して使用率1.9%） |

---

## 今後やること・ロードマップ

1. **スマホ（LINE）× デスクトップ連携Botの構築**
   - LINE Messaging APIのチャンネル作成
   - デスクトップ側常駐スクリプトの作成（LINEから「アイデア」「投稿案作成」を受信）
   - Cloudflare Tunnel等で安全に接続
2. **Day 9以降のX投稿コンテンツ準備**
   - `docs/CONTENT_PIPELINE.md` に候補政策をストック
3. **新規機能のプロトタイピング（必要に応じて）**
   - 政策のお気に入り保存機能（LocalStorage）
   - 政策比較機能など